// Reverse

// Explicame que hace la funcion reverse y como se usa
// La funcion reverse es una funcion que se encarga de invertir el orden de los elementos de un array.
// La funcion reverse se usa para invertir el orden de los elementos de un array.

// Ejemplo de como se usa la funcion reverse
const array = [1, 2, 3, 4, 5]
console.log(array.reverse())

// Ejemplo de como se usa la funcion reverse con un string
const string = 'Hello'
console.log(string.split('').reverse().join(''))